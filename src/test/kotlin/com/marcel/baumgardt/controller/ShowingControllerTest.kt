package com.marcel.baumgardt.controller

class ShowingControllerTest {
    //---BY ADMIN---
    //Update price of showings
    //Update price of showings - Exception
    //Update price of all showings by CinemaId,MovieId
    //Update price of all showings by CinemaId,MovieId - Exception

    //Update dates of showing (Replace) - CinemaId,MovieId,Map<0-6,List<HH:mm>>
    //Update dates of showing (Add) - CinemaId,MovieId,List<Int> 0-6,List<HH:mm>
    // Update dates of showing (Replace) - Exception - CinemaId,MovieId,List<Int> 0-6,List<HH:mm>
    //Update dates of showing (Add) - Exception - CinemaId,MovieId,List<Int> 0-6,List<HH:mm>
    //---BY ADMIN---

    //---BY USER---
    //Get showing dates return (CinemaName,MovieName,Map<0-6,List<HH:mm>>) , request(CinemaId,MovieId)
    //---BY USER---
}